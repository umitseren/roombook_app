# scripts/check.conf — the steps behind the single verification contract (./scripts/check).
# Generated by the bootstrap workflow for: Python 3.13 + FastAPI + SQLModel + SQLite.
# One step per line:  <name>: <command>   — runs in order, fails fast.
#
# GREENFIELD NO-OP: no application code exists yet, so the steps below are commented out and
#   ./scripts/check exits 0 as a greenfield no-op. When the first feature scaffolds pyproject.toml
#   + the app/ packages and installs the dev toolchain, uncomment ALL the steps below.
#
# Prereqs (once, on the first feature):
#   python -m venv .venv && . .venv/Scripts/activate   (Windows: PowerShell)
#   pip install -e ".[dev]"
#
# --- Uncomment ALL four lines below when the first feature scaffolds the app + toolchain ---
# lint:    ruff check app tests
# types:   mypy app
# test:    pytest -q --cov=app --cov-fail-under=80
# audit:   pip-audit --strict
